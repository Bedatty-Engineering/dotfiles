[default]
region = us-east-1

### Company
[sso-session acme-sso]
sso_start_url = https://<SSO_SUBDOMAIN>.awsapps.com/start#
sso_region = us-east-1
sso_registration_scopes = sso:account:access

[profile acme]
sso_session = acme-sso
sso_account_id = <ACCOUNT_ID_MAIN>
sso_role_name = AWSAdministratorAccess
region = us-east-2
output = json
cli_pager =

[profile acme-devops]
sso_session = acme-sso
sso_account_id = <ACCOUNT_ID_DEVOPS>
sso_role_name = AWSDevopsAdministratorAccess
region = us-east-2
output = json
cli_pager =

[profile acme-network]
sso_session = acme-sso
sso_account_id = <ACCOUNT_ID_NETWORK>
sso_role_name = AWSDevopsAdministratorAccess
region = us-east-2
output = json
cli_pager =

[profile acme-development]
sso_session = acme-sso
sso_account_id = <ACCOUNT_ID_DEVELOPMENT>
sso_role_name = AWSDevopsAdministratorAccess
region = us-east-2
output = json
cli_pager =

[profile acme-production]
sso_session = acme-sso
sso_account_id = <ACCOUNT_ID_PRODUCTION>
sso_role_name = AWSDevopsAdministratorAccess
region = us-east-2
output = json
cli_pager =

[profile acme-sandbox]
sso_session = acme-sso
sso_account_id = <ACCOUNT_ID_SANDBOX>
sso_role_name = AWSDevopsAdministratorAccess
region = us-east-2
output = json
cli_pager =
